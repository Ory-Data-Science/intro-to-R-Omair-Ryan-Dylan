help("abs")
# abs(x) computes the abs value of x, sqrt(x) computes the (principal) square root of X. 
help("round")
#ceiling takes a single numeric argument x and returns a numeric vector containing the smallest integers not less than the corresponding elements of x.floor takes a single numeric argument x and returns a numeric vector containing the largest integers not greater than the corresponding elements of x.Error: unexpected symbol in "floor takes"
help("tolower")
help("toupper")
# same thing, Translate characters in character vectors, in particular from upper to lower case or vice versa.
abs(-15)
print(abs(-15))
round(4.483847, digits = 1)
species <- "SPECIES"
tolower(species)
# result ="species"
 toupper(species)
# result = "SPECIES"
abs_value <- sqrt(2.6)
abs_rounded_value <- (round(abs_value, digits = 2))
print(abs_rounded_value)
# [1] 1.61
print(round(sqrt(2.6), digits = 2))
# 1.61